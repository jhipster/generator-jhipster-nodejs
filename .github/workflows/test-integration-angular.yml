name: Test Integration Angular
on:
  push:
    branches-ignore:
      - 'dependabot/**'
  pull_request:
    branches:
      - '*'
permissions:
  contents: read
env:
  NODE_OPTIONS: --dns-result-order=ipv4first
  FORCE_COLOR: 2
jobs:
  samples:
    name: ${{ matrix.app-type }}
    runs-on: ${{ matrix.os }}
    if: "!contains(github.event.head_commit.message, '[ci skip]') && !contains(github.event.head_commit.message, '[skip ci]')"
    timeout-minutes: 40
    defaults:
      run:
        working-directory: ${{ github.workspace }}/app
    strategy:
      fail-fast: false
      matrix:
        node-version: [20]
        os: [ubuntu-latest]
        app-type:
          - monolith-angular-jwt-jdl
          - monolith-angular-jwt-i18n-jdl
          - monolith-angular-oauth2-jdl
          - monolith-angular-oauth2-mongodb-jdl
          - monolith-angular-oauth2-i18n-jdl
          - monolith-angular-jwt-i18n-dev
          - monolith-angular-jwt-mongodb-jdl
          - monolith-angular-mssql-prod-jdl
          - monolith-angular-postgresql-prod-jdl
        include:
          - app-type: monolith-angular-jwt-jdl
            app-folder: monolith-client-auth-template-jdl
            entity: import-jdl
            e2e: jwt
          - app-type: monolith-angular-jwt-mongodb-jdl
            app-folder: monolith-angular-auth-mongodb-template-jdl
            entity: import-jdl
            e2e: jwt
          - app-type: monolith-angular-jwt-i18n-jdl
            app-folder: monolith-client-auth-i18n-template-jdl
            entity: import-jdl
            e2e: jwt
          - app-type: monolith-angular-oauth2-jdl
            app-folder: monolith-client-auth-template-jdl
            entity: import-jdl
            e2e: oauth2
          - app-type: monolith-angular-oauth2-mongodb-jdl
            app-folder: monolith-angular-auth-mongodb-template-jdl
            entity: import-jdl
            e2e: oauth2
          - app-type: monolith-angular-oauth2-i18n-jdl
            app-folder: monolith-client-auth-i18n-template-jdl
            entity: import-jdl
            e2e: oauth2
          - app-type: monolith-angular-jwt-i18n-dev
            app-folder: monolith-angular-jwt-i18n-dev
            entity: 'no'
            e2e: jwt
          - app-type: monolith-angular-mssql-prod-jdl
            app-folder: monolith-client-database-prod-template-jdl
            entity: import-jdl
            e2e: jwt
          - app-type: monolith-angular-postgresql-prod-jdl
            app-folder: monolith-client-database-prod-template-jdl
            entity: import-jdl
            e2e: jwt
    env:
      NHI_APP: ${{ matrix.app-type }}
      NHI_APP_FOLDER: ${{ matrix.app-folder }}
      NHI_ENTITY: ${{ matrix.entity }}
      NHI_E2E: ${{ matrix.e2e }}

    steps:
      - uses: actions/checkout@v4
        with:
          path: generator-jhipster-nodejs
      - uses: jhipster/actions/setup-runner@v0
        with:
          node-version: ${{ matrix.node-version }}
          binary-dir: ${{ github.workspace }}/generator-jhipster-nodejs/cli/
      - run: npm install
        working-directory: ${{ github.workspace }}/generator-jhipster-nodejs
      - run: cli.cjs generate-sample ${{ matrix.app-type }}
      - uses: jhipster/actions/compare-sample@v0
        id: compare
        if: >-
          github.event.pull_request &&
          !contains(github.event.pull_request.labels.*.name, 'pr: disable-compare')
        with:
          generator-path: generator-jhipster-nodejs
          cmd: cli.cjs generate-sample ${{ matrix.app-type }} --skip-jhipster-dependencies --skip-install ${{ matrix.extra-args }}
      - name: 'INSTALL DEPENDENCIES of generated app'
        run: npm install
      - name: 'RUN UNIT TESTS of generated app'
        run: npm test --workspaces
      - name: 'RUN SERVER E2E TESTS of generated app'
        run: npm run test:server:e2e
      - name: 'BUILD AND RUN SERVER APP'
        run: npm run app:up
      - name: 'START APP WITH EVENTUAL CLIENT E2E TESTS'
        run: npm run e2e:headless --workspace client
        id: e2e
      - name: Store cypress screenshots
        uses: actions/upload-artifact@v4
        if: always() && steps.e2e.outcome == 'failure'
        with:
          name: screenshots-${{ matrix.app-type }}
          path: ${{ github.workspace }}/app/**/cypress/screenshots
      - name: Dump docker logs
        if: always()
        uses: jwalton/gh-docker-logs@v2
  check-angular-applications:
    permissions:
      contents: none
    runs-on: ubuntu-latest
    needs: [samples]
    if: always()
    steps:
      - run: |
          echo '${{ toJSON(needs) }}'
          if [ 'skipped' == '${{ needs.samples.result }}' ] || [ 'success' == '${{ needs.applications.result }}' ] || [ 'closed' == '${{ github.event.action }}' ]; then
            exit 0
          fi
          exit 1
