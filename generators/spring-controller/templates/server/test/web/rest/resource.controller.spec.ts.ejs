import { Test, TestingModule } from '@nestjs/testing';
import { <%= controllerClass %>Controller } from '../../../src/web/rest/<%= controllerFileName %>.controller';

describe('<%= controllerClass %> Controller', () => {
  let controller: <%= controllerClass %>Controller;

  beforeEach(async () => {
    const module: TestingModule = await Test.createTestingModule({
      controllers: [<%= controllerClass %>Controller],
    }).compile();

    controller = module.get<<%= controllerClass %>Controller>(<%= controllerClass %>Controller);
  });

  it('should be defined', () => {
    expect(controller).toBeDefined();
  });
});
