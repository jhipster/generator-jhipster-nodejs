import { Module } from '@nestjs/common';
import { TypeOrmModule } from '@nestjs/typeorm';
import { <%= entityClass %>Controller } from '../web/rest/<%= entityModelFileName %>.controller';
import { <%= entityClass %>Repository } from '../repository/<%= entityModelFileName %>.repository';

@Module({
  imports: [TypeOrmModule.forFeature([<%= entityClass %>Repository])],
  controllers: [<%= entityClass %>Controller],
})
export class <%= entityClass %>Module {}
