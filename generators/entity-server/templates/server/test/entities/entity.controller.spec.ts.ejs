import { Test, TestingModule } from '@nestjs/testing';
import { <%= entityClass %>Controller } from '../../src/web/rest/<%= entityFileName %>.controller';
import { <%= entityClass %>Repository } from '../../src/repository/<%= entityFileName %>.repository';

describe('<%= entityClass %> Controller', () => {
  let controller: <%= entityClass %>Controller;

  beforeEach(async () => {
    const module: TestingModule = await Test.createTestingModule({
      controllers: [<%= entityClass %>Controller],
      providers: [<%= entityClass %>Repository]
    }).compile();

    controller = module.get<<%= entityClass %>Controller>(<%= entityClass %>Controller);
  });

  it('should be defined', () => {
    expect(controller).toBeDefined();
  });
});
